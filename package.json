{
  "name": "resil",
  "version": "0.0.2",
  "main": "build/index.js",
  "packageManager": "yarn@3.6.3",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "test": "jest",
    "prepublish": "yarn build && yarn test"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/node": "^20.5.9",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": "18.16.0",
    "yarn": ">=3"
  },
  "dependencies": {
    "fputils": "^1.2.10"
  }
}
